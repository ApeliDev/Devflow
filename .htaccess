RewriteEngine On

# Make sure the requested URL is not a real directory or file
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f

# Rewrite the URL by appending .php
RewriteRule ^([a-zA-Z0-9_-]+)$ $1.php [L]
